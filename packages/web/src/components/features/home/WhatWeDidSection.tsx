import React from "react";

interface Props {
  // define your props here
}

const WhatWeDidSection: React.FC<Props> = ({}) => {
  return <section>WhatWeDidSection</section>;
};

export default WhatWeDidSection;
