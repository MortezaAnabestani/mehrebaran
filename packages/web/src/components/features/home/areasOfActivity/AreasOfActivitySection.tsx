import React from "react";
import AreaItem from "./AreaItem";

interface Props {
  // define your props here
}

const AreasOfActivitySection: React.FC<Props> = ({}) => {
  return (
    <section>
      <AreaItem />
    </section>
  );
};

export default AreasOfActivitySection;
